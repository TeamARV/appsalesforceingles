   1 Copia tu proyecto completo, incluyendo el archivo requirements.txt, en una carpeta nueva en la PC de destino.

    2 Instala Python y pip en la PC de destino. Puedes descargar Python desde la página oficial de Python
    (https://www.python.org/downloads/) y pip se instala automáticamente junto con Python.

     Notas : 
        Paso 1 - Instalar python con el PATH habilitado
        Paso 2 - Validar en cmd que este instalado python con python --version
        paso 3 - Validar pip --version 

        Forzar instalacion 
        paso 5 - instalar entorno virtual en alguna carpeta ---> metodo forzado python -m virtualenv venv
        paso 4 - python -m pip install virtualenv // virtualenv --version
        
        para devmode
        paso 6 - select interpreter en vscode ---> F1

    3 Crea un nuevo entorno virtual en la PC de destino con el mismo intérprete de Python que usaste en tu entorno virtual original. Puedes crear un nuevo entorno virtual ejecutando el comando python -m venv env en la carpeta del proyecto.


    4 Activa el entorno virtual 

    5 Instala las dependencias y paquetes necesarios para el proyecto desde el archivo requirements.txt ejecutando el comando pip install -r requirements.txt en la carpeta del proyecto.



    6 Ejecuta el proyecto de Django en la PC de destino ejecutando el comando python manage.py runserver en la carpeta del proyecto.




devmode

// opcionales
vstudio
complementos python
complementos material icon

riveravegaandres@wise-hawk-b8wjs3.com

+pastdostrescuatroX939
+pastdostrescuatroX9392
+pastdostrescuatroX93922
